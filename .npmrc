# pnpm configuration
auto-install-peers=true
strict-peer-dependencies=false
shamefully-hoist=false

# Ignore Prisma peer dependencies from drizzle-orm (we use postgres driver instead)
ignore-workspace-peer-dependencies=@prisma/client,prisma

# Use workspace protocol for internal dependencies
link-workspace-packages=true
