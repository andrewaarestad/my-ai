# Environment variables for My AI web app

# ==============================================================================
# ENVIRONMENT DETECTION
# ==============================================================================
# NODE_ENV is automatically set by Next.js (development, production, test)
# VERCEL_ENV is set by Vercel deployments (development, preview, production)
#
# The app uses these to determine error display behavior:
# - development: Full error details with stack traces and troubleshooting
# - preview: Full error details (Vercel preview deployments)
# - production: Generic user-friendly error messages
#
# You don't need to set these manually - they're set automatically by the platform

# ==============================================================================
# DATABASE (REQUIRED)
# ==============================================================================
# Vercel Postgres or Supabase connection string
# This is the pooled connection URL for application queries
DATABASE_URL="postgresql://user:password@host:5432/database?sslmode=require"

# Direct URL for migrations (required for Vercel Postgres)
# This is the direct (non-pooled) connection for running Prisma migrations
DIRECT_URL="postgresql://user:password@host:5432/database?sslmode=require"

# ==============================================================================
# NEXTAUTH.JS CONFIGURATION (REQUIRED)
# ==============================================================================
# Secret key for NextAuth.js session encryption
# Generate a random secret with: openssl rand -base64 32
# IMPORTANT: Keep this secret and never commit it to version control
NEXTAUTH_SECRET="your-secret-key-here"

# Base URL of your application
# Development: http://localhost:3000
# Preview: https://your-preview-url.vercel.app
# Production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# ==============================================================================
# GOOGLE OAUTH PROVIDER (REQUIRED)
# ==============================================================================
# Get these from Google Cloud Console: https://console.cloud.google.com/
#
# Setup Instructions:
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select an existing one
# 3. Enable Google+ API
# 4. Go to "Credentials" and create OAuth 2.0 Client ID
# 5. Add authorized redirect URIs:
#    - Development: http://localhost:3000/api/auth/callback/google
#    - Preview: https://your-preview-url.vercel.app/api/auth/callback/google
#    - Production: https://yourdomain.com/api/auth/callback/google
#
GOOGLE_CLIENT_ID="your-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-client-secret"

# ==============================================================================
# OPTIONAL: ADDITIONAL OAUTH PROVIDERS (for future use)
# ==============================================================================
# GITHUB_CLIENT_ID=""
# GITHUB_CLIENT_SECRET=""
# MICROSOFT_CLIENT_ID=""
# MICROSOFT_CLIENT_SECRET=""

# ==============================================================================
# TROUBLESHOOTING
# ==============================================================================
# If you see authentication errors:
# 1. Verify all REQUIRED variables above are set
# 2. Check that NEXTAUTH_URL matches your current domain
# 3. Ensure OAuth redirect URIs in Google Cloud Console match NEXTAUTH_URL
# 4. Verify DATABASE_URL and DIRECT_URL are correct and database is accessible
# 5. In development/preview, you'll see detailed error messages with troubleshooting steps
# 6. Check server logs for additional error details
