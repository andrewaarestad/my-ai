{
  "buildCommand": "pnpm turbo run build --filter=@my-ai/web",
  "devCommand": "pnpm dev",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "crons": [
    {
      "path": "/api/gmail/cron",
      "schedule": "*/15 * * * *"
    }
  ]
}
